const db_name = "";
const db_user = "";
const db_password = "";
const db_host = "";
const db_port = ;
const db_dialect = "";
const sequelize_log = ;
const port = ;
const private_key = "";

const dbConfig = {
  host: db_host,
  user: db_user,
  password: db_password,
  database: db_name,
  port: db_port,
};

export default {
  private_key,
  db_name,
  db_user,
  port,
  db_password,
  db_host,
  db_port,
  db_dialect,
  sequelize_log,
  dbConfig,
};
